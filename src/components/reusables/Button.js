import react, { Component } from 'react';
import { View } from 'react-native';

const Button = () => {
    return (
      <View>
        <button></button>
      </View>
    );
};

export default Button;
